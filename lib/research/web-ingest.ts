// Intentionally empty: we no longer ingest raw webpages.
// Web research is stored as "research_step_output" documents (notes + citation links),
// and only that output is chunked/embedded.
export {};
