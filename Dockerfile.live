FROM node:20-slim

WORKDIR /app

# Same pattern as Dockerfile.worker: `tsx` lives in devDependencies; include dev for runtime `tsx`.
ENV NODE_ENV=production

COPY package*.json ./
RUN npm ci --include=dev

COPY . .

EXPOSE 8080

CMD ["npm", "run", "live-assistant-server"]
