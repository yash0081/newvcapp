/**
 * Offline keyword cluster refresh (global medoid refinement).
 * Full DSU/offline ANN merge is not implemented here — reserved for batch jobs.
 */
console.log("refresh-keyword-clusters: no-op stub; run after accumulating new phrases in production.");
